# Бэклог задач

Крупные блоки работ. `✅` — выполнено, `⬜` — в очереди. Формулировки — как их дал автор
(слегка причёсано, суть не тронута); брать из списка по мере надобности. Внутри блока после
цитаты — короткий **Итог:** с тем, что реально легло в код.

**Фундамент** (до нумерации, ✅): новая стейт-машина с ангаром корабля как хабом; камера —
RMB-поворот (вертикаль инвертирована) + LMB-пан вдоль пола с `panRect`; модульный корабль и
полностью автоматический бой прямо в ангаре; ресурс `Metal`, `RES_LIVES` убран; диалоги и
обзор планет вырезаны, красный маркер планеты = «там враги».

---

## 1. Шейдер планет и звёзд — ✅

> Давай сделаем шейдер планет / звёзд — это должен быть трипланарный шейдер, где мы можем
> задавать 3 скейла (для каждой плоскости — отдельно), на вход подаём текстуру шума со
> скейлом и сдвигом, смешиваем и компенсируем искажения (где пересекаются пропорционально).
> Текстура — шум, генерируем простенький многослойный шум перлина: берём средний цвет и
> накладываем на него несколько случайных величин (от −1 до +1), интерполированных синусом, с
> масштабом 1/4, 1/8, 1/16 и т.д. Классический шум, возможно есть готовые решения. И кроме
> того, передаём однопиксельную текстуру — карту высот (цвета градиента). В параметр добавить
> самосвечение (чтобы можно было так же делать солнце — просто градиент другой). Приготовить
> несколько градиентов: зелёно-синие планетки, серые луны, красные марсы и жёлто-оранжевые,
> сине-белые солнышки. Чтобы сделать Юпитер (газовые гиганты) — шум сжать вдоль вертикальной
> оси, будет похоже на газовые слои. Приготовь инфраструктуру для простого создания текстур
> из массивов (интерпретируем строку как дискретизированные элементы), предсоздай архетипы:
> вариации размеров, планет, газовых гигантов, спутников, разумный список градиентов.

**Итог:** `texture.js` — `GenNoiseTexture` (tileable value-noise, косинус-интерп, октавы
1/4…1/16) + `GenRampTexture`/`GetRamp` (градиент из строки, 3 символа = RGB-стоп). `objects.js`
— `planetProgram` (`objVS`→трипланар по `objPos*uPlaneScale + uOffset + gTime*uDrift`, веса
`abs(n)^4`, рампа = высота→цвет, `mix(lit, glow, uEmissive)`, рим от `camEyePos`). `s3`-статы
не тут: `PLANET_ARCHETYPES` (`system.js`) и `STAR_ARCHETYPES` (`galaxy.js`) — `ramp`/
`planeScale`/`drift`/`emissive`. Газовые гиганты `planeScale [0.16, 9, 0.16]` → полосы. См.
[texture.md](texture.md), [architecture.md](architecture.md#планеты-и-звёзды--трипланарный-шейдер).

---

## 2. Модель корабля: схемы-строки, слот-апгрейд, статы, модульные враги, UI-блоки, дебаг-меню — ✅

> Непостроенные корабли (фрегаты) должны быть пустыми (ничего не отображается). Контекстное
> меню должно скрываться когда выбрал оружие или ещё раз кликнул на слот. Пульки летят из
> точки оружия — приподнятой над центром клетки (там же, где башня у корабля и у корвета).
> Вражеский корабль — такой же модульный. При стрельбе конечная точка — не центр врага, а
> случайная клетка + случайная точка в её объёме.
>
> Новый важный слот — апгрейд самого корабля: меняет центральный слот с мешем корабля и
> списком всех оффсетов, а также параметры (больше HP, больше слотов разных типов —
> переменные, как ресурсы). Любой компонент может менять эти переменные (щиты → ёмкость щита).
> Сбоку показываем максимум: возможно до 20 слотов, но видно только первые X из переменной
> своего типа (оружие / модули (ПВО и щиты) / корабли). С апгрейдом число растёт (кроме
> кораблей поддержки — их можно строить сразу, это тактический выбор). 3 типа корабля из
> кубиков: 1 — 3×3, центральный блок вперёд, 1 пушка по центру средней полосы, 2 защитных по
> центру боковых, 2 слота поддержки вокруг; 2 — 5×5 `01000/11110/01111/11110/01000`
> (крылатый); 3 — 5×5 `11100/01110/11111/01110/11100`.
>
> Слоты налезают друг на друга и разной ширины — сделать блоками постоянной ширины и
> контролируемой высоты (20 кнопок должны занять ровно всю высоту подряд), поуже по высоте.
> Дебажные функции собрать в дебаг-меню — маленькая кнопка (`D`) вверху, открывает список
> команд, легко добавлять (отдельный модуль): спавн кораблей разных типов, +металл, +топливо.
>
> Пометить в доках и `.claude`: обновлять доки по мере имплементации, последние команды —
> самые верные, комментировать неочевидное не стесняясь (вырезается при сборке).

**Итог:** схема-строка `<W><./#/W/M сетка>|<коорд. поддержки>` — `DecodeShipSchema`
([shipformat.md](shipformat.md)), один декодер на игрока и врагов. Центральный `SLOT_SHIP` →
фреймы `s1/s2/s3` (`schema` + базовые `stats`), `BuildShip` → `rebuildSlots` с переносом
модулей. `shipStats` (`STAT_HP/WEAPONS/MODULES/SUPPORTS/SHIELD`) = база фрейма + `stats`
модулей; `SlotCap`/`ActiveSlots` режут список по потолку. Пустые слоты — без 3D. Меню
закрывается по постройке / повторному клику. Турели стреляют из `pos+(0,0.45,0)` в
`scatter(cells)`. Враги — красные схема-кубики, «Carrier» + фрегаты со своим HP. UI —
`.slotbtn` фикс-размер (после `.button`/`.panel` в каскаде); левый список = полоска HP
корабля + слот Ship + группы Weapons/Modules/Support с зазором (текстовые заголовки убраны
позже), правый — колонка полосок HP врагов (см. [ui.md](ui.md)). `debug.js` —
`CreateDebugMenu`, `DEBUG_GLOBAL` + команды от состояния. Доки: `.claude/CLAUDE.md` + `architecture.md` — правило «доки вместе с кодом,
комментировать не жалко».

---

## 3. Время строительства, раскладка кнопок, эффекты перелёта, 2 масштаба системы, звёздное небо — ✅

**Итог Части A:** очередь сборки (`builds` в `modules.js`, `TickBuilds`/`PendingBuild`) —
металл списывается сразу, модуль встаёт через `buildTime` (тир 2с / корвет 3.5с / разборка
0.6с / фрейм 5с), строка слота показывает `▸Ns` + полоску прогресса. Кнопки: пассив
`bottomleft` / актив `bottomright` (Map, Return to ship — слева; Jump, `>` — справа).
`hyperjump_state` переписан — радужный тоннель из колец кубиков (`GenRingOfCubes`), цвет по
позиции вдоль тоннеля, залипает у камеры, ресайкл вперёд, гнутые стенки, корабль в центре.
`system_travel_state` — систему видно (планеты как фон), мимо летят вытянутые искры. Оба:
3 фазы доворота камеры (`LerpAngle` в `gl.js`) по направлению полёта и обратно. Звёздное
небо — `CreateStarfield`, рисуется каждый кадр в `main.js` до `ctx.objects`.

**Доработка флоу (фидбек):** звезда — полноценная локация. Клик по солнцу в карте системы
больше не телепортирует в галактику — солнце выбирается как цель прыжка (как планета), после
`Jump` игрок припаркован на солнечной орбите (`sys.parkedPlanet = PARK_STAR`, `system.js`).
В `ship_state` на солнечной орбите доступна кнопка `Hyperjump` (справа) → карта галактики
(с кольцом радиуса прыжка вокруг текущей звезды) → выбор системы → снова эффект гиперпрыжка
в ship-виде → прибытие на солнечную орбиту новой системы. Транзит-стейты
(`hyperjump_state`/`system_travel_state`) рисуют **настоящий модульный корабль игрока**
(`CreateShipModelObjects` в `modules.js`), не плейсхолдер; камера — как в `ship_state`.
Тоннель гиперпрыжка — плотнее (22–24 сегм. в кольце).

**Доработка 2 (фидбек):** зафиксирована конвенция осей — x лево/право, y верх/низ, z
вперёд/назад, плоскость движения xz, нос корабля в −z (см.
[architecture.md](architecture.md#оси)). Эффекты — вдоль **мировой оси z**, не forward
камеры (позже локальный перелёт переведён на курс `fwd`, см. ниже). Схемы `s2`/`s3`
перерисованы симметричными стрелками (были повёрнуты на 90°). В `ship_state` всегда виден
фон текущей системы (планеты + спутники, без взаимодействия).

**Часть B (сделано):** `DecoSystem` (`system.js`) — отдельные инстансы системы в
декорационном масштабе (радиусы ×5, расстояния раздуты `decoDist`, LOD по радиусу). Корабль
на «пятачке» (origin), систему двигаем через `PlaceDeco(sys, body)` / `DecoVicinity`.
`ship_state` рисует декорацию у пятачка припаркованного тела; `system_travel_state
(onDone, fromBody, toBody)` едет всей декорацией от одного пятачка к другому за время эффекта.
Карта (`system_state`) — по-прежнему свой, тесный масштаб. Дебаг-меню: `Preview: local jump` /
`Preview: hyperjump` для просмотра эффектов; тоннель гиперпрыжка ещё плотнее, эффекты длиннее.

**Часть B-2 — истинный масштаб (фидбек):** декорация раздута с ×5/×7 до **истинного**
масштаба — `DECO_RADIUS` 1000 (планета ~200-300 юнитов), `DECO_ORBIT` 400 (тела на
~800-3600 юнитов, с зазором), звезда скромнее (`DECO_STAR_RMUL`). `DecoVicinity` — пятачок
теперь **на орбите** тела (сдвиг по касательной на радиус+зазор), гигант нависает сбоку и
занимает пол-экрана. `camera.js`: `near` 0.3, `far` 10000. LOD в `decoCopy` растёт как
√radius. `system_travel` — искры и камера под новый масштаб (длинные полоски, скорость 700),
в фазе крейсера можно вертеть камерой (RMB) — смотреть, как покинутый гигант уменьшается, а
целевой растёт.

**Часть B-3 — направление эффекта (фидбек):** локальный перелёт теперь честно по **курсу**
`fwd = norm(vTo − vFrom)`: (1) корабль довора­чивается на `fwd` за фазу въезда (`RotY` по
кубикам, `RotY` добавлен в `gl.js`), (2) камера садится позади по `fwd` и прижимается
(`radius` 34→18), (3) искры-полоски летят вдоль `fwd` (растянуты по доминантной оси), яркость
по огибающей фазы, (4) система едет `vFrom→vTo` **только** в крейсере, (5) прилетели — искры
гаснут, (6) камера отходит. Раньше искры шли вдоль мировой z, независимо от курса. Шум остаётся масштабо-независимым (выборка на единичной сфере — см.
[architecture.md](architecture.md)), поэтому узор планеты одинаков на карте (радиус 0.2) и
вблизи (250). **Звёздное небо** переведено в скайбокс: оболочка на `R≈7500` (кубики
пропорционально крупнее), `main.js` каждый кадр центрирует её на глазе камеры — нулевой
параллакс, система пролетает мимо. Карта не тронута.

> Полный текст задачи ниже.

> У слотов добавить время строительства — пару секунд, ~5 секунд для корпуса корабля.
> Поменять местами кнопку прыжка и возврата на корабль (и карту на экране корабля): пассивное
> действие снизу слева, активное — справа.
>
> Эффекты переделать. При полёте разворачивать камеру: смотреть туда же, куда корабль в
> горизонтальной плоскости (с разницей ~10°), с небольшим подъёмом по вертикали (не в
> двигатель, а чуть выше — 10–15°). После перелёта камеру вернуть. Желательно отдельным
> стейтом — проще тюнить. Корабль оставить, остальное скрыть (пока просто выключать).
>
> Гиперпереход — «плазма»: светящийся радужный тоннель, уходящий в темноту, со стенками.
> Пока проще: плотные ряды концентрических колец из кубиков, каждый ряд — цвет радужного
> градиента, градиент движется вместе с кольцами. Стартовые кольца чёрные, приближаясь —
> набирают цвет, потом цвет фиксируется и едет, пока не скроется за спиной → ресайкл вперёд.
> Пока без сдвигов, потом можно шум.
>
> Локальный перелёт (между планетами) — ничего не прятать, игрок видит систему. Показываем
> летящие из темноты и мимо корабля искры (длинные полоски) — просто эффект.
>
> Систему уметь показывать в 2 масштабах: **карта** (как сейчас) и **декорация** — увеличить
> в десятки раз, позиции корабля = реальные точки рядом с планетами (красивый вид, не
> вертеть, висим рядом). Двигать не корабль, а систему (корабль на пятачке). Масштаб для
> «реальной» системы, вероятно, другой (менее карикатурный): большая планета рядом, мелкие
> вдали, звезда где-то там. Пригодится разная детализация (у больших планет больше
> треугольников). Планеты процедурные — не страшно. Переход между планетами анимируем честно:
> двигаем всю систему, показываем эффект, за пару секунд «долетаем» до орбиты другой планеты.
> Камера — так же: прижать по направлению, потом вернуть.
>
> Звёздное небо: X координат в кубе × масштаб, удалить точки ближе Min / дальше Max
> (сферически-равномерное распределение без звёзд рядом). По точкам — скайбокс из белых
> квадратиков разного размера. В идеале — текстура: квадрат с радиальным градиентом от
> чёрного к белому из центра.

---

## 4. Оптимизация размера — 🔄 в работе (итеративно)

**Репорт:** [optimization.md](optimization.md) — замеры, «где байты», что не работает,
бэклог с оценками.

**Заход 5 — план (шаги от автора):**

*Шаг 1 (сделано, −140 Б):*
- `MeshCharVal(code)` → `MeshCharVal(s, i)` — всегда звался на символе строки, `charCodeAt`
  убран из 6 колл-сайтов внутрь функции.
- новый **`include.js`** (первый в конкатенации): `Math`-алиасы, `GL_*`-энумы, `F32`, и
  `let gl` — declaration в первом файле видна всем последующим (TDZ закрывается сразу,
  значение приходит из `InitGL` при старте). Из `gl.js` эти блоки убраны.
- **`gl` — глобаль**: параметр `gl` вырезан из ~19 фабрик/`Init*` и ~70 колл-сайтов
  (`CreateXxx(ctx.gl, …)` → `CreateXxx(…)`), `ctx.gl` → `gl`. `ctx` больше не носит `.gl`.

*Шаг 2, часть A (сделано, ~0 Б — архитектурный дедуп):* `MeshGen()` в `geometry.js` —
собирает квады (`quad(a,b,c,d)`), опционально через матрицу (`xf(m)`, `Mat4MulPoint` в
`gl.js`), декодит строку (`str(s)`); `build()` раскладывает фейсы по порядку добавления +
одна плоская нормаль на квад. Единственная таблица куба — `CUBE_QUADS`. `GenCubeMesh` /
`GenRingOfCubes` / `GenEncodedCubeMesh` (`DecodeMeshString`) теперь штампуют её через
`MeshGen`. Проверено: меш-данные байт-в-байт, оба билда рендерят. Байтов не выиграли (дедуп
съеден оверхедом построителя), но база под выразительные корабельные меши готова.

*Шаг 2, часть B (отложено): сфера как quadsphere.* Посчитать «крышку» (диск `+0.1y`, xz
`-1..1`) один раз, нормализовать, добавить 6× под матрицами поворота (нужны `Mat4RotX/Y` +
`Mat4Multiply`). Риск визуальный (силуэт планет, LOD в `decoCopy`), байтовый выигрыш неясен
(судя по части A — вряд ли большой) → отдельным проходом со скринами до/после.

**Заход 4 (−366 Б в релизе):**
- **баг-фикс:** закавычены ключи `MODULES`/`FIRE_COLORS`/`SHIP_PROJ_SPEED`/`SHOOT_SFX` —
  closure `ADVANCED` их переименовывал, минифицированная сборка падала на «Play»
  ([build.md](build.md)). `build.py` пишет `index.dev.html` (неминифицированный) для отладки.
- **дебаг-тулинг вырезан из релиза (−329 Б):** `const DEBUG`, `if (DEBUG) CreateDebugMenu(…)`,
  `build.py` гасит флаг перед closure → DCE всего дебаг-меню.
- **короткие CSS-имена (−37 Б):** `#ui`→`#u`, `#hud`→`#h`, `bottomleft/…/rightcol`→`bl/…/rc`,
  `slotbtn/slotlife`→`sb/sf`.
- мимо: `function`→стрелки (0/+4 Б), `V3(0,0,0)`→`V3()` (0 Б, оставлено), `CreateHoldButton`
  удалён (был мёртвый). Про «сжатие 60 %» — это метрика типизации closure, не компрессия;
  реальный deflate минифицированного JS ~58 %, это норма (см. [optimization.md](optimization.md)).

**Заход 3 — сделано (−126 Б zip):** WebGL-энумы → числовые `GL_*` в `gl.js` (спец Khronos,
рантайм-сверка 24 значений), `F32()` вместо `new Float32Array`, камера `update/rotate/zoom`
→ `upd/rot/zm` (closure их не манглит — DOM-externs). **Главный вывод:** обёртки-сокращалки
(строковые константы, ключи данных, обёртки методов Web Audio) = **0 Б** — closure инлайнит
литералы, gzip жмёт повторы. Полная таблица проверенных «мимо» — [optimization.md](optimization.md).

**Заход 2 — сделано (−26 Б zip, 14619 → 14593):** дедуп `vizColor`→`shipVizColor`, новый
`PushObjects(ctx, list)` вместо 7 циклов push. `GenNoiseTexture` 2 октавы — проверено, 0 Б,
отклонено. Детали — [optimization.md](optimization.md).

**Заход 1 — сделано (−234 Б zip, 14853 → 14619):**
- Широкий строковый алфавит через `charCodeAt` (`MeshCharVal`/`MeshEncChar`, 93 уровня,
  нечётное → точные −1/0/+0.5/+1) — убран `const MESH_ALPHABET` (лежал в бандле 6 копий),
  `DecodeMeshString` + `GenRampTexture` через него, перегенерированы `CUBE_MESH` и 14 строк
  рамп.
- Ручная минификация всех 4 GLSL-шейдеров (`objects.js`), читаемые версии — в комментариях.
- Алиасы `Math.*` в `gl.js` + свип по файлам (закрепили идиому; экономия мизерная — closure
  инлайнит обратно, см. репорт).

Зип всё ещё **1307 Б over** — остальное по бэклогу [optimization.md](optimization.md)
следующими заходами.


> У нас будет ещё много штук, которые надо сократить — игра должна быть компактной. Предложи,
> что можно подрезать, оптимизировать, улучшить, переиспользовать — алиасы функций, более
> оптимальная запись структур, всё что придёт в голову. Составь репорт в документах.
>
> Идея автора: функция «строка → массив с сильной дискретизацией» сейчас буквально объявляет
> весь алфавит — не компактно и против идеи. Улучшить: взять непрерывный диапазон ASCII,
> конвертируемый через `ord(char)`; пара запрещённых символов в начале — не проблема
> (маленькие числа, тоже пригодятся). Расширить с 64 до максимума вменяемых печатных символов
> (кажется, ~96), взять весь рабочий диапазон; разрывы — либо через `if`, либо взять диапазон
> поменьше без разрывов. В комментарии показать весь ряд и отметить важные значения (0, 1,
> 0.5). Возможно взять нечётное число уровней, чтобы дискретизация давала ровный 0.5 (он же 0
> в диапазоне −1…1). Может понадобиться несколько версий декодера (по-разному трактовать
> массив).

---

## 5. Звук и музыка — 🔄 первый проход

**Репорт:** [sound.md](sound.md) — обзор подходов (jsfxr / ZzFX+ZzFXM / sonant / TinyMusic /
свой), почему выбран **свой мини-синт** (правило «без сторонних либ», один модуль на SFX +
музыку, ~1.4 КБ, тащим сразу).

**Сделано:** `html/js/sound.js` — примитивы `Tone`/`Noise` (осциллятор+ADSR / фильтрованный
шум), пресеты `Sfx.{click,build,hit,explosion,jump,hyperjump,death}` + `SHOOT_SFX` по
`fireKind`, тягучий 16-шаговый эмбиент (2 дрона + тик + бас + редкие минорные ноты,
`MusicStart`/`MusicStop`), `ToggleMute` в дебаг-меню. Разблокировка `AudioContext` — по
первому клику кнопки. Точки вызова расставлены в `ui.js` / `ship_state.js` /
`system_travel_state.js` / `hyperjump_state.js` / `menu_state.js` (таблица в репорте).
Проверено в браузере: контекст поднимается по жесту, планировщик музыки идёт, все SFX без
ошибок, консоль чистая. Зип **16 075 / 13 312** (+1.4 КБ — оптимизация в блоке 4).

**Срез размера (блок 4, −586 Б):** амбиент-музыка **вырезана** (стоила ~360 Б, была самой
сырой частью) — `MusicStart`/`MusicStop` оставлены пустыми функциями, трек вернётся с
бюджетом. `Tone`/`Noise` переведены с объекта-опций на позиционные аргументы (−224 Б):
music-only ручки `attack`/`sustain`/`at`/`bus` и узел `_musicBus` убраны, attack и «колено»
decay зафиксированы. Все 10 SFX на месте. Зип 15 689 → **15 103**.

**Осталось (бэклог в [sound.md](sound.md)):** вернуть музыку (когда влезет), разнообразие
клика, финальная настройка параметров на слух.

> Финальный пока не делаем, но нужно попробовать. Составь репорт: какие библиотеки/подходы
> знаешь. Оптимум — `jsfxr` (автор сам подберёт эффекты для кнопок, выстрелов, перелётов —
> инструмент знаком). Музыка: либо простой сэмплер на базе того же `jsfxr`, либо другой.
> Можно попробовать сочинить простенький тягучий тикающий трек — скорее саспенс, чем трек,
> для настроения. Возможно есть готовые трекеры для «потыкать». Плохо, если понадобится 2
> библиотеки — даже упакованные тяжёлые; тем важнее втащить их сразу.
